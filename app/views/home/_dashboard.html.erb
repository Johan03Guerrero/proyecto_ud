<% params[:active] ||= 'principal' %>
<div class="div-principal">
  <%= render "layouts/flash_messages" %>
  <ul class="nav nav-pills nav-fill" id="pills-tab" role="tablist">
    <li class="nav-item" role="presentation">
      <button class="nav-link <%= 'active' if params[:active] == 'principal'%>" id="pills-principal-tab" data-bs-toggle="pill" data-bs-target="#pills-principal" type="button" role="tab" aria-controls="pills-principal" aria-selected="true">Informes Principales</button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link <%= 'active' if params[:active] == 'personalizada'%>" id="pills-personalizado-tab" data-bs-toggle="pill" data-bs-target="#pills-personalizado" type="button" role="tab" aria-controls="pills-personalizado" aria-selected="false">Generar Informe Personalizado</button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link <%= 'active' if params[:active] == 'proyeccion'%>" id="pills-proyeccion-tab" data-bs-toggle="pill" data-bs-target="#pills-proyeccion" type="button" role="tab" aria-controls="pills-personalizado" aria-selected="false">Proyección</button>
    </li>
  </ul>
  <div class="tab-content" id="pills-tabContent">
    <div class="tab-pane fade <%= 'show active' if params[:active] == 'principal'%>" id="pills-principal" role="tabpanel" aria-labelledby="pills-principal-tab">
      <%= render partial: 'dashboard_principal' %>
    </div>
    <div class="tab-pane fade <%= 'show active' if params[:active] == 'personalizada'%> " id="pills-personalizado" role="tabpanel" aria-labelledby="pills-personalizado-tab">
      <%= render partial: 'dashboard_personalizado' %>
    </div>
   <div class="tab-pane fade <%= 'show active' if params[:active] == 'proyeccion'%> " id="pills-proyeccion" role="tabpanel" aria-labelledby="pills-personalizado-tab">
      <%= render partial: 'dashboard_proyeccion' %>
    </div>
  </div>
</div>